interface SystemBasic {
    basic: {
        domain_name: string;
        site_name: string;
        sq_email: string;
        use_dormant: string;
        use_external_email: string;
        use_smtp: string;
    },
    image: {
        image_resize: string;
        image_width_max: string;
    }
}

interface SystemProfile {
    use_foasdf: number;
}
